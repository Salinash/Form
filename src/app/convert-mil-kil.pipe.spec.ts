import { ConvertMilKilPipe } from './convert-mil-kil.pipe';

describe('ConvertMilKilPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMilKilPipe();
    expect(pipe).toBeTruthy();
  });
});
